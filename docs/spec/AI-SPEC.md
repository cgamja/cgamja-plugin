# AI Spec

## AI Agent
- **`Claude Code` only**

Codex가 안좋은 시절부터 `Claude Code`를 써오기도 했고, 그만큼 생태계가 아직은 `Codex`보다 높을 거라고 생각한다.

그리고 `Claude Code` 사용을 숙련하고나서 `Codex`를 쓰든 같이 쓰든 하는 것이 좋다고 판단했다.

## MCPs
- **Figma MCP**

피그마 코드 읽기 및 스냅샷을 위해 연결

<br>

- **JIRA MCP**

JIRA로 협업을 하고있고(2026-08-30 기준), 후에도 사용할 수 있으므로 연결해놓는다. 추후 필요 없을 때 비활성화 한다.

<br>

- **Notion MCP**

기본 비활성화한다. 보통 보고서, 트러블슈팅 등을 적기 위해 연결해놓는다.

## Skills

적절한 Skill들을 적재적소에 활용하기 위해, 프론트엔드 개발, 리뷰, 디자인에 도움되는 스킬들을 모아놓았다.

### 코드 설명

-  **eli5** (https://github.com/dreambigou/eli5)

5살한테 설명하는 것처럼 친절하게 설명해준다. 이해용으로 중간중간 사용자가 쓰는 용도

<br>

- **project-artifact** (https://github.com/anthropics/claude-plugins-official/tree/main/plugins/project-artifact)

Generate and publish a living status page for a project that's too big for one update — a migration, a launch, a research effort, anything with several workstreams tracked over time. The page is a single self-contained tabbed HTML file (overview & success criteria, the workstream sequence, an always-visible "Next steps" strip, plus background / plan / risks / FAQ tabs when they earn their place), published with Claude Code's built-in Artifact tool to a private claude.ai/code/artifact/... page that you can share with teammates.

<br>

###  디자인
<br>

- **`frontend-design`** (https://github.com/anthropics/claude-plugins-official/tree/main/plugins/frontend-design)

Generates distinctive, production-grade frontend interfaces that avoid generic AI aesthetics.

<br>

- **`taste-skills`** (https://github.com/Leonxlnx/taste-skill)

The Anti-Slop Frontend Framework for AI Agents

<br>

- **`frontend-ui-engineering`** (https://github.com/addyosmani/agent-skills/blob/main/skills/frontend-ui-engineering/SKILL.md)

Build production-quality user interfaces that are accessible, performant, and visually polished. The goal is UI that looks like it was built by a design-aware engineer at a top company — not like it was generated by an AI. This means real design system adherence, proper accessibility, thoughtful interaction patterns, and no generic "AI aesthetic."

### 개발
<br>

- **OpenSpec** (https://github.com/Fission-AI/openspec)

**SDD(Spec-Driven Deevelopent)** 기반으로 해야 일관된 코드가 나올 수 있다.

이 때 `Spec-Kit`의 무거운 CLI 보일러플레이트나 `Compound Engineering`의 복잡한 에이전트 오버헤드와 비교했을 때, `OpenSpec`은 불필요한 런타임 종속 없이 순수 파일 구조만으로 LLM에 명확한 명세와 태스크를 강제할 수 있는 가장 가볍고 이식성 높은 SDD 도구라고 판단했다.

<br>

- **`typescript-lsp`** (https://github.com/anthropics/claude-plugins-official/tree/main/plugins/typescript-lsp)

TypeScript/JavaScript language server for Claude Code, providing code intelligence features like go-to-definition, find references, and error checking.

<br>

- **`code-review`** (https://github.com/anthropics/claude-plugins-official/tree/main/plugins/code-review)

Automated code review for pull requests using multiple specialized agents with confidence-based scoring to filter false positives.

<br>

- **`claude-security`** (https://github.com/anthropics/claude-plugins-official/tree/main/plugins/claude-security)

Put a team of agents to work as security researchers on your codebase: map the architecture, build a threat model, hunt across every component, and independently verify every finding before it reaches the report. Then, if you want, turn the confirmed findings into suggested fixes delivered as targeted patch files you review and apply when you choose.

<br>

- `react-native-skills` (https://github.com/vercel-labs/agent-skills/tree/main/skills/react-native-skills)

A structured repository for creating and maintaining React Native Best Practices optimized for agents and LLMs.

- `composition-patterns` (https://github.com/vercel-labs/agent-skills/tree/main/skills/composition-patterns)

A structured repository for React composition patterns that scale. These patterns help avoid boolean prop proliferation by using compound components, lifting state, and composing internals.

- **`reviewer-cgamja`** (`../.claude/skills/review-cgamja/`)

코드 변경분이 내 코드 철학(PHILOSOPHY.md + docs/)에 부합하는지 reviewer-cgamja 서브에이전트로 검사하고, 위반이 있으면 수정 후 재검사까지 돌리는 게이트. 사용자가 "철학대로 됐는지 봐줘", "철학 리뷰", "cgamja 리뷰", "내 규칙 지켰는지 확인해줘"라고 하거나, 구현 작업을 마치고 커밋/PR 직전에 철학 준수를 확인해야 할 때, 또는 /review-cgamja 호출 시 사용한다. 일반적인 버그 찾기 리뷰가 아니라 "이 레포의 문서화된 철학과의 대조"가 목적일 때 이 스킬이다.

<br>

### 테스트
<br>

- **`browser-testing-with-devtools`** (https://github.com/addyosmani/agent-skills/blob/main/skills/browser-testing-with-devtools/SKILL.md)

Use Chrome DevTools MCP to give your agent eyes into the browser. This bridges the gap between static code analysis and live browser execution — the agent can see what the user sees, inspect the DOM, read console logs, analyze network requests, and capture performance data. Instead of guessing what's happening at runtime, verify it.

<br>

### QA
<br>

- **`qa-cgamja` (`../.claude/skills/qa-cgamja/`)

<br>

### Context Engineering
<br>

- **`context-engineering`** (https://github.com/addyosmani/agent-skills/blob/main/skills/context-engineering/SKILL.md)

Feed agents the right information at the right time. Context is the single biggest lever for agent output quality — too little and the agent hallucinates, too much and it loses focus. Context engineering is the practice of deliberately curating what the agent sees, when it sees it, and how it's structured.

<br>

- **`test-driven-development`** (https://github.com/addyosmani/agent-skills/blob/main/skills/test-driven-development/SKILL.md)

Write a failing test before writing the code that makes it pass. For bug fixes, reproduce the bug with a test before attempting a fix. Tests are proof — "seems right" is not done. A codebase with good tests is an AI agent's superpower; a codebase without tests is a liability.

<br>

### Optimization
<br>

- `performance-optimization` (https://github.com/addyosmani/agent-skills/blob/main/skills/performance-optimization/SKILL.md)

Optimizes application performance across frontend, backend, queries, and databases. Use when performance requirements exist, when you suspect performance regressions, when Core Web Vitals or load times need improvement, when N+1 query patterns need fixing, or when profiling reveals bottlenecks.\

<br>

### Observation
<br>

- **`observability-and-instrumentation`** (https://github.com/addyosmani/agent-skills/blob/main/skills/observability-and-instrumentation/SKILL.md)

Instruments code so production behavior is visible and diagnosable. Use when adding logging, metrics, tracing, or alerting. Use when shipping any feature that runs in production and you need evidence it works. Use when production issues are reported but you can't tell what happened from the available data.
