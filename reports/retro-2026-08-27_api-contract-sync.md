# retro 2026-08-27 — api-contract-v1-sync 단일 세션 감사

**판정: 세션 1개(실작업 약 1시간 53분, 스냅샷 시점 미종료 가능성) · 마찰 18건(개별) — 최다: 훅 deny 9건(오탐 6·67%) · 재검토 조건 발동 2개 · ADR 후보 4개(2개는 당일 앞선 retro-2026-08-27.md 후보를 보강, 2개는 신규).**

- 실행: retro-fe(adr/0022) — 사용자 지정 단일 세션(선택 절차 생략, 대상이 명시됨). 세션당 분석 서브에이전트(sonnet) 1개, 본체 세션 모델.
- 대상 프로젝트: `~/app`(simsimee-studio, Expo RN), 브랜치 `chore/api-contract-sync`.
- 대상 세션은 같은 날 먼저 나온 `retro-2026-08-27.md`(SIM-145 세션 4개)와 **다른 세션**이다 — 겹치지 않는다.
- 세션 파일이 감사 중에도 계속 append됨(2385줄 지시 → 스냅샷 시점 2430줄+) — 세션이 분석 스냅샷 이후에도 이어졌을 가능성 있음. 스냅샷 이후 구간은 이 보고에 없음.

## 대상 세션

| 세션 | 성격 | 실작업 | 커밋 | 종료 방식 |
|---|---|---|---|---|
| ee7057d9 | `docs/ENDPOINTS.md` 정합 — `api-contract-v1-sync` change(앨범 커서 무한조회·월 그룹·204 처리 + 걱정 대화 서버 주도 `next_question`)를 계약 v1로 전면 이행 | ~113분(스냅샷 시점) | 13(1 amend) | 스냅샷 시점 진행 중(PR 미도달, 마지막 관측이 미해결 사용자 메시지) |

## 마찰 집계 (신호별)

| # | 신호 | 건수 | 대표 증거 |
|---|---|---|---|
| 1 | 훅 deny — `[protect]` 훅우회금지 2·인터프리터 1·계약생성물 3·의존성쉘변경 2 / `[tdd]` 테스트파일쉘 1 | 9 | `ls src/api/client.gen.ts && ...` 명령이 경로만 언급했다고 `[protect] 계약 생성물` 차단(04:09:51) |
| 1-오탐 | 위 중 읽기 전용인데 차단된 것 | **6/9 (67%)** | hooksPath 단순 조회 2건, `node -e` package.json scripts 읽기 1건, `ls`/`grep`로 생성물 경로만 참조 2건, `git add`+`git commit`을 "쉘 편집"으로 오판 1건 |
| 2 | 사용자 인터럽트 | 4 | 3건이 02:41~02:44 4분 내 몰림(스펙 propose 단계 앨범/걱정 택일 논의): "아니 백엔드한테 요청할 수 있어. 두 방법 중에 뭐가 낫냐고"(02:41:13). 1건은 증거 수집 중 화면 오류 놓침: "아니 지금 error가 떠있다고"(04:04:30) |
| 3 | 동일 도구 연속 실패(엄격 기준: 같은 입력 반복) | 0 | — |
| 3-근접 | Edit "string not found" — 다른 파일·다른 시각, 매번 재읽기 후 1회 재시도로 해소 | 4 | spec.md(03:08), concern-chat/spec.md(03:24), month-groups.test.ts(03:42), concern-chat-session.test.tsx(03:44) |
| 4 | 게이트·절차 이탈 — 우회 명령(`git clean`/`--no-verify`/`git reset --hard`) | 0 | — |
| 4-순서역전 | review-fe L5(플랫폼) 렌즈가 "증거 누락(스크린샷 0장)"을 blocker로 먼저 지적 → 증거 수집이 리뷰보다 늦게 시작 | 1 | "L5 도착 — blocker는 증거 누락(E1: 프로필 요구 스크린샷 0장) 1건"(03:35:11) |
| 5 | 도구 오류(is_error) | Bash 차단 9(신호1과 중복)·Edit not-found 4·Bash exit1(의도된 red 확인) 2·Read not-found 1 | — |
| 6 | 시간 | 총 ~113분. 스펙 확인·propose(02:27~02:44, 인터럽트 3) → red/feat 앨범·걱정 반복(02:59~03:28) → **review-fe 6렌즈(03:28:31)** → 렌즈 도착(03:34:57~03:40:36) → fix(review)(03:36~03:50) → 재검증(03:51) → 증거 수집(03:51~04:19, 메트로 번들러 오류 트러블슈팅 포함) | 리뷰~증거~재검증 구간(51분)이 전체의 45% |
| 7 | 커밋/PR/승인프롬프트 | 커밋 13(1 amend) · `gh pr create` 0회 · `permissionDecision` 필드는 이 로그에 0건 관측(아래 메모 참조) | — |
| 8 | 리뷰 렌즈 | Agent 호출 10회 — L1×2(초기+재검증)·L2×2·L3×1·L4×2·L5×1·L6×2(+Explore 1). 6렌즈 전원 blocker 보유(합산 9건, "없음" 렌즈 0개). `fix(review)` 1개로 일괄 반영 후 blocker 있던 4렌즈 재실행 | "6개 렌즈 전부 도착 — blocker 9건"(03:40:36) |

**메모(신호 7 관련, retro-fe 자신에 대한 관찰)**: 이 세션 로그에는 `permissionDecision` 필드가 없고, 훅 deny는 tool_result 텍스트의 `[protect]`/`[tdd]` 접두 문자열로만 나타났다. 실제 UI "ask" 승인 프롬프트 횟수는 이 파일 하나로는 확인 불가 — retro-fe 스킬의 §신호 문서가 가정하는 마커와 이 프로젝트 훅 구현의 실제 출력 채널이 어긋난다(adr/0022 재검토 조건 "트랜스크립트 포맷 변경으로 파싱이 깨짐"에 해당할 수 있음 — 포맷이 바뀐 게 아니라 애초에 이 채널을 안 쓰는 훅 구현일 수도 있어 확인 필요).

## 재검토 조건 대조 (workflow.md 7장)

| 조건 | 판정 | 근거 |
|---|---|---|
| 훅을 우회하는 습관 → 훅 범위 축소 | **발동**(문맥상 근접 — 우회가 아니라 오탐이 원인, 당일 앞선 retro-2026-08-27.md와 동일 재해석) | 신호 1-오탐: 6/9건(67%)이 읽기 전용 명령 차단. 같은 날 다른 세션 감사에서도 "오탐이 마찰의 ~70%"로 집계 — 두 세션이 서로 다른 시각·다른 작업인데 같은 오탐 계열이 반복 |
| change당 커밋 10개 초과 2번 | **발동(3번째 데이터 포인트)** | 이 세션 13개(> 10). 같은 날 앞선 retro의 SIM-139(42)·0101f9e8(15+)에 이어 세 번째 초과 사례. 단 이번 세션은 CI 라운드 재작업이 아니라 **정상 Tier-2 절차 안에서** 초과(두 도메인 × red/feat + review fix + spec converge + deps 정리) — 초과 원인이 앞선 사례와 다르다 |
| Tier-2 스펙 작성이 구현보다 오래 걸린 게 2번 | 데이터 부족(이 세션 단독으로는 스펙 propose 17분 vs 구현 44분 — 구현이 더 오래 걸림, 미발동 방향이나 표본 1개) | — |
| 스펙이 테스트된 동작과 모순된 채 출하된 change 2개 | 데이터 부족(archive 여부 미확인) | — |
| 리뷰 지적 3회 연속 스타일뿐 / 렌즈별 "없음" 3연속 | 미발동 | 6렌즈 전원 blocker 보유 — 반대로 "지적 과다"에 가까움 |
| sonnet 구현이 같은 스펙에서 2회 실패 → opus 승격 | 미발동 | Edit not-found 4건 모두 1회 재시도로 해소, red 테스트 실패는 의도된 것(exit1 2건) |
| 같은 실패가 docs/solutions에 2번 | 데이터 부족 | 대조 미수행 |
| 오래된 스냅샷/Figma 50회 | 해당 없음 | 이 세션 Figma 호출 0회 |

## ADR 후보

| # | 제목 | 한 줄 근거 | 상태 |
|---|---|---|---|
| 1 | 훅 오탐 4계열 정밀화 | 당일 다른 세션(retro-2026-08-27.md)에서 이미 제안된 후보와 동일 계열 — 이 세션(6/9=67% 오탐)이 근거를 보강 | **보강**(신규 아님) |
| 2 | 커밋 10개 상한을 Tier-2 도메인 수 기준으로 조정 검토 | 이 세션은 CI 재작업이 아니라 정상 절차(도메인 2개 × red/feat)만으로 13개 — 단일 도메인 change 기준의 상한이 다중 도메인 change엔 구조적으로 안 맞을 수 있음 | **신규** — workflow.md 5장 커밋 절 |
| 3 | review-fe 호출 전 증거(스크린샷 등) 존재를 사전 게이트로 강제 | 신호 4-순서역전: L5가 "증거 누락"을 리뷰 도중 발견 — workflow.md Tier-2 4→5단계 순서(구현·증거 완료 후 리뷰)가 실행에서 지켜지지 않음. 발생 1회뿐이라 ADR보다 관찰로 남기는 편이 안전(같은 이탈 2회 규약 미충족) | **관찰만**(2회째 재발 시 ADR 후보로 승격) |
| 4 | retro-fe §신호의 훅 마커 문서를 프로젝트 실제 채널에 맞춰 갱신 | `permissionDecision` 필드가 이 프로젝트 로그에 없음 — retro-fe 스킬 자신의 파싱 가정과 훅 구현 실제 출력이 어긋남(adr/0022 재검토 조건) | **관찰**(retro-fe 스킬 references 갱신 후보, 코드 ADR 아님) |

## lens-ledger 제안 행 (직접 쓰지 않음 — adr/0016)

- `2026-08-27 | api-contract-v1-sync(ee7057d9) | L1~L6(opus/sonnet persona) ×10(초기6+재검증4) | 합산 blocker 9 · "없음" 렌즈 0개 | fix(review) 1커밋으로 일괄 반영 후 4렌즈 재검증 통과`

## 관찰 (수정 안 함)

- 메트로 번들러 "Unable to resolve" 오류로 증거 수집 중 `metro.config.js`를 즉석 작성 — 당일 다른 세션(retro-2026-08-27.md #9 "dev 환경 갭")과 같은 계열, 세 번째 관측이면 dev 연결 레시피 ADR 근거 추가
- 스펙 propose 단계 인터럽트 3건(4분 내 집중, 앨범/걱정 접근 택일) — openspec-propose가 확정 전 큰 산출물을 한 번에 내놓아 사용자가 개입 지점을 못 잡고 인터럽트로 끊음. 경미, 단일 사례

## 다음

- ADR 후보 2번(커밋 상한 재검토)이 이 세션 고유 — 사람이 workflow.md 5장 커밋 절과 함께 판단 필요.
- 후보 3·4번은 재발 시(같은 이탈 2회, adr/README 규약) 정식 ADR로 승격.
- 세션이 스냅샷 시점 진행 중이었다 — 종료 후 재감사하면 CI/PR 단계 신호가 추가될 수 있음.
