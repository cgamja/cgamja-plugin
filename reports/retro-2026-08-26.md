# retro 2026-08-26 — SIM-141·SIM-139 실사용 세션 3개 감사

**판정: 세션 3개(약 7.7시간 분량) · 마찰 20건(계열 병합 기준, 훅 오탐 12회 포함) · 재검토 조건 발동 2개(둘 다 0018·0019로 기반영) · ADR 후보 3개(신규).**

- 실행: retro-fe(adr/0022) 2회차 — 세션당 분석 서브에이전트(sonnet) 3개 병렬, 본체 fable.
- 대상 프로젝트: `~/app`(simsimee-studio, Expo RN). 요청 트리거: 사용자 "SIM-139 하나에 커밋 105개" 체감 + CI 리뷰 라운드 상한(5회) 도달.
- 커밋 수 사실 정정: 105는 브랜치 `git log` 전체(=dev 64 + 고유 42)를 센 것. **SIM-139 고유 커밋은 42개**(본작업 33 + CI 라운드 8 + 병합 1), SIM-141은 49개.

## 기준선 — "이미 반영됨" 판별

| 시각(KST) | 사건 |
|---|---|
| 8/25 15:59~18:48 | 세션 A: SIM-141 develop-fe 본작업 |
| 8/26 14:55~17:51 | 세션 B: SIM-139 develop-fe 본작업 → PR #3 |
| 8/26 17:52~19:45 | 세션 C: SIM-139 PR 리뷰·CI 라운드 1~5 반영 |
| 8/26 19:41~19:49 | 플러그인 개선 웨이브 머지(PR #3~#8, adr/0017~0022) |
| 8/26 19:53 | `~/app` develop-setup 재실행(훅 재설치) |

**세 세션 모두 0017~0022 반영 전 플러그인으로 수행됐다.** 아래 마찰 중 상당수는 이 웨이브가 이미 고쳤고(코드 대조로 확인), 그 항목은 "반영됨(재측정)"으로 표기한다 — 다음 Tier-2 실측에서 사라졌는지 재야 한다.

## 대상 세션

| 세션 | 기간(UTC) | 크기 | 내용 | 커밋 | 승인/경고 |
|---|---|---|---|---|---|
| A `d8c94ffd` | 8/25 06:59→09:48 (2h49m) | 19MB | SIM-141 앨범 홈 — 스펙→red 18개→구현→증거→리뷰→PR #2 | ~24 | 세팅 누락 advisory **145회**, AskUserQuestion 7회 |
| B `8d5a9b3e` | 8/26 05:55→08:51 (2h55m) | 14MB | SIM-139 기록 남기기 — 티어→스펙→TDD→렌즈 리뷰 2라운드→PR #3 | 33 | [tdd] ask **47회**, deny 11회(오탐 3) |
| C `7efd5099` | 8/26 08:52→10:45 (1h37m) | 2.8MB | SIM-139 CI 리뷰 라운드 1~5 반영 | 8 | [tdd] ask **40회**, deny 2회(전부 정탐) |

※ 위임 시 A를 SIM-139로 추정했으나 트랜스크립트·커밋 대조 결과 SIM-141이었다. 결과적으로 change 2개(Tier-2 ×2)의 비교 데이터가 됐다.

## 세션 간 집계 — 비용(시간 낭비·빈도) 순

| # | 마찰 계열 | 세션·규모 | 분류 | 상태 |
|---|---|---|---|---|
| 1 | **[tdd] red 게이트 ask가 테스트 파일 편집마다 발생** — 승인 87회(B 47 + C 40), 거부 0. 라운드가 늘수록 선형 누적, 사용자가 C에서 "일괄 커밋" 오버라이드 | B·C | 스킬 설계 | **반영됨(adr/0018** 세션당 1회 red-mark**) — 재측정** |
| 2 | **CI 리뷰 5라운드 미수렴** — 새 버그가 아니라 결함 계열 2개(①EXIF 없는 사진의 게이트·라벨·문구 일관성 R1→R3 재등장 ②재시도 진행 키가 불안정한 `uri` R1→R5 재등장)가 "지적된 표면만 최소 수정"되며 반복. R3 수정이 단일 영상 케이스를 빠뜨려 R4 유발 | C 96.8분 전체 | 모델 행동 + 스킬 설계 | **미해결 → ADR 후보 1** |
| 3 | **`~/app` 세팅 누락 advisory 145회 반복**(verify·config·rules·Stop훅 부재 경고) | A | 프로젝트 세팅 | **해소됨** — 8/26 19:53 develop-setup 재실행. 재측정 |
| 4 | **시뮬레이터 증거 수집 31분·Bash 재시도 29회**(기기 미발견, sleep 체인 차단, 타임아웃 7분·4분) | A 최대 구간 | 프로젝트 세팅 + 모델 행동 | **반영됨(adr/0021** evidence-capture·동선 반전**) — 재측정** |
| 5 | **protect-bash 오탐 3종**: ①`git config core.hooksPath` 조회를 우회로 오판 ②커맨드 전체 문자열에서 보호파일명·WRITE 동사를 독립 매칭(무관한 tasks.md write가 package.json 위반으로) ③`git commit[^|]* -n `이 `&&` 뒤 `sed -n`까지 그리디 매칭 | B 3회+재시도 | 스킬 설계 | ① 0017 반영 확인. **②③ 현재 훅에 잔존(코드 대조) → ADR 후보 2** |
| 6 | **review_nudge(Agent 훅) 오탐 9/10** — review-fe가 띄운 정상 렌즈 에이전트(`subagent_type: cgamja:reviewer-*`)를 "즉석 리뷰"로 오인. 화이트리스트가 `agents/reviewer-` 경로 문자열만 검사 | A | 스킬 설계 | **잔존(코드 대조) → ADR 후보 3** |
| 7 | 보호 파일(tailwind.config.js) 하드차단 시행착오 — 승인받고도 Edit·Bash 연속 거부, 스크래치 작성→사람 `cp`까지 8분. 2회차엔 세션 메모리로 1분 | B | 스킬 설계 | **반영됨(adr/0017** protected→ask**) — 재측정** |
| 8 | **날짜 선택 UI 재작업 ~1시간** — datetimepicker 설치·구현·캡처 완료 후 사용자 지시("국내식 바텀시트 캘린더")로 전면 교체. Tier-2 게이트 질문이 인터랙션 패턴까지 확정 안 함 + Figma에 해당 세부 부재 가능성 | B | 스킬 설계 + 프로젝트 세팅 | 1회 — 관찰(2회째면 게이트 질문에 '인터랙션 패턴' 항목 추가) |
| 9 | 라운드 상한 도달 시 마지막 라운드 수정이 재검증 없이 종결(R5 수정 → CI 기계 체크만 초록, 내용 재검토 없음) | C | 프로젝트 세팅(~/app CI 정책) | 관찰 → ADR 후보 1에 병합 |
| 10 | lint max-lines(300) 게이트에 Edit 3연속 실패, jscpd 중복도 사후 하네스 추출 | C | 프로젝트 세팅 | 관찰 1회 |
| 11 | 커밋 단위 마찰 — 사용자 정정 "테스트 고치면 하나씩 커밋하지 말고 한꺼번에/기능 단위로"(인터럽트 동반) | C | 스킬 설계 | **반영됨(adr/0019** 단계 커밋**)과 방향 일치 — 재측정** |
| 12 | force-push 필요성 사전 미설명 → 사용자 되물음("왜 굳이 force로") — commitlint 소급 위반 정정에 히스토리 재작성이 불가피했음 | C | 모델 행동(보고 갭) | 관찰 1회 |
| 13 | PR 직전 "자체 검증 5항목"을 스킬 밖 raw `general-purpose` Agent로 수행 — 프로젝트 AGENTS.md에 스킬 계약 밖 게이트가 자라남 | A·B | 프로젝트 세팅 | 관찰 — 스킬 흡수 여부는 데이터 더 필요 |
| 14 | 환경 결함: orval 생성물 zod 버전 불일치 1회, jest 29/jest-expo 57/msw ESM/RNTL 13 조합 사전 미검증(러너 수리 커밋) | A | 프로젝트 세팅 | docs/solutions 기록됨(각 1회) |
| 15 | 모델 습관(경미·반복): 테스트/보호 파일을 Bash(heredoc·sed) 먼저 시도→거부→Edit 전환 반복, Edit old_string 불일치 5회(A2+B3), 장시간 sleep 체인 시도 3회(A2+C1), JSX 주석 구문 오류 2연속 | A·B·C | 모델 행동 | 관찰 — 훅이 전부 잡음(설계 의도대로) |

훅 **정탐**(정상 차단, 마찰 아님): lefthook `no-test-edit-in-feat` 1회(A, 즉시 분리 재커밋), [tdd] 쉘 편집 차단 1회(C), [protect] force-push 차단 1회(C, 사람이 별도 터미널로 실행 — 설계 의도). **진짜 우회 시도는 3세션 통틀어 0건.**

## 재검토 조건 대조 (workflow.md 7장 + ADR 재검토 조건 절)

| 조건 | 판정 | 근거 |
|---|---|---|
| change당 커밋 10개 초과 2번(0019) | **발동** | SIM-141 ~24개 + SIM-139 41개(33+8). 단 두 change 모두 0019 반영 전 수행 — 규칙 위반이 아니라 규칙 부재기의 데이터. 0019 적용 후 change에서 재측정 |
| ask 10회 이상 피로(0009→0018) | **발동** | [tdd] ask 87회. 0018이 정확히 이 데이터로 이미 채택됨 — 이번 감사는 사후 검증 |
| IWRITE·protect 오탐/누락 각 2회(0017) | 판정 보류 | 오탐 3건 전부 0017 이전 실행. 코드 대조로 ②③ 잔존 확정 → 조건 발동을 기다릴 것 없이 ADR 후보 2로 |
| 증거 단계 20분 초과 2회(0021) | 1/2 | A 32분 1회 확정. B는 구현 구간에 섞여 분리 불가(데이터 부족). 0021 동선 반전 후 재측정 |
| 훅을 우회하는 습관 | 미발동 | 우회 시도 0건. deny 정탐 3건은 전부 규약대로 해소 |
| Tier-2 스펙 작성 > 구현 2번 | 미발동 | A 22m vs 21m(근접 1회), B 28m vs 92m |
| 리뷰 지적 3연속 스타일뿐 / 렌즈 "없음" 3연속 | 미발동 | B: R1 blocker 3, R2 신규 blocker 4 — 실질 지적 |
| 같은 실패 docs/solutions 2번 | 미발동 | fake timers·SDK 매트릭스 각 1회 |
| sonnet 구현 같은 스펙 2회 실패 / flaky 주2회 / 도메인 엣지 3개 / 스냅샷 오구현 2번(0002) / Figma 재손질 2번(0003) | 미발동 | 해당 사례 없음 |
| 실험 B(ce-work 브리지, 0001) 2 change 후 기록 | 데이터 부족 | 세션에서 브리지 사용 흔적 미확인 — 실험 자체가 미개시로 보임 |
| 0018 "실패 원문 없이 test 커밋 2개" | 미발동 | red 커밋 전부 실패 요약 포함(예: "red 8/8(미구현)") |

## ADR 후보

1. **리뷰 지적 반영은 결함 계열 단위로 — CI 라운드 수렴 규칙** — 근거: 집계 #2(계열 2개가 최소 수정으로 R1→R3, R1→R5 재등장, 상한 5회 소진) + #9(상한 도달 시 마지막 수정 무검증). 방향: 리뷰 반영 단계에 "지적 케이스만 고치지 말고 같은 조건식·인접 케이스 매트릭스(사진/영상 × 단일/다중)를 함께 판정" 지시 + 라운드 상한 도달 시 마지막 수정에 L1 1회 재검증. 파일: `skills/review-fe/SKILL.md`(반영 단계), `skills/develop-fe/workflow.md` 2장 리뷰 배치, (프로젝트 쪽) `~/app` CI re-review 정책.
2. **protect-bash 정규식 스코핑 잔여 2종** — 근거: 집계 #5 ②③(현재 훅 코드 25~26행·9행에 잔존 확인). 방향: 명령을 `&&`·`;` 경계로 분리해 명령 단위로 보호파일·WRITE 상관 매칭, `git commit ... -n`은 명령 경계 내로 한정. 파일: `skills/develop-setup/templates/hooks/protect-bash.sh`, `tests/test_setup_hooks.sh`(오탐 3건을 회귀 케이스로). 0017 재검토 조건("오탐 2회 → 패턴 조정")의 연장.
3. **review_nudge 화이트리스트 확장** — 근거: 집계 #6(9/10 오탐). 방향: `agents/reviewer-` 경로 외에 `subagent_type`의 `cgamja:reviewer-*`도 정상 경로로 인정. 파일: `skills/develop-fe/hooks/review_nudge.sh` 6행, `tests/test_develop_hooks.sh`. advisory라 피해는 컨텍스트 노이즈지만 세션당 ~10회면 교정 가치 있음.

## lens-ledger 제안 행 (reports/lens-ledger.md, adr/0016 — 직접 기입하지 않음)

- 2026-08-26 SIM-139(B): R1 렌즈 6개 → 지적 ~30건(blocker 3) 전량 반영 / R2 렌즈 4개 → 신규 blocker 4건 red 선행 후 반영 / R3 규칙상 생략. **이후 CI 리뷰가 렌즈가 못 잡은 결함 계열 2개(EXIF 게이트 일관성, 재시도 키 안정성)를 5라운드에 걸쳐 발견** — L1 정확성 렌즈의 케이스 매트릭스 커버리지 갭 신호.

## 다음 실측에서 잴 것 (재측정 체크리스트)

0017~0022 반영 + 훅 재설치 후 첫 Tier-2 change에서: ① [tdd] ask 횟수(87→세션당 1 기대) ② 세팅 advisory 0인지 ③ 증거 단계 소요(32분→20분 미만) ④ change당 커밋 수(0019 단계 커밋으로 10개 근처) ⑤ protect 오탐(ADR 후보 2 반영 전이면 ②③ 유형 재발 예상).
