# retro 2026-08-27 — SIM-145 실사용 세션 4개 감사

**판정: 세션 4개(실작업 약 2.4시간) · 마찰 10계열(개별 ~60건 — 최다: advisory 경고 스팸 31회, 린트 반복 차단 13회, 훅 오탐 deny 3건) · 재검토 조건 발동 2개 · ADR 후보 5개.**

- 실행: retro-fe(adr/0022) 3회차 — 세션당 분석 서브에이전트(sonnet) 4개 병렬, 본체 fable. 서브에이전트 토큰 합 ≈350k.
- 대상 프로젝트: `~/app`(simsimee-studio, Expo RN). 직전 회고(retro-2026-08-26)가 SIM-141·139를 커버했으므로 이번은 그 이후 SIM-145 계열.
- 현재 세션(0101f9e8)은 **진행 중 스냅샷**을 분석했다 — 스냅샷 이후 구간(CI 2라운드 후반)은 본체 직접 관찰로 보완(§본체 보완).

## 대상 세션

| 세션 | 성격 | 실작업 | 커밋 | 종료 방식 |
|---|---|---|---|---|
| 3b8e61e1 | SIM-145 후속 Tier-1 ×3 사이클(아이콘/FAB·fetcher baseUrl·키보드 스크롤) | ~79.5분 | 2 | **사용자 인터럽트**(Android 증거 캡처 중, 3번째 수정 미커밋) |
| 0101f9e8 | SIM-145 후속 Tier-1(저장 표시 제거·헤더 뒤로가기) + CI 리뷰 2라운드 + archive | ~55분+ | 15+ | 진행 중(retro 호출) |
| 719ed3fc | 순수 Q&A(orval 생성물 질문) | 107초 | 0 | 사용자 인터럽트 ×2 |
| 2f2c3931 | 순수 Q&A(JIRA 읽기 범위 질문) | 4분 | 0 | 정상 |

## 마찰 집계 (계열 병합, 빈도·낭비 순)

| # | 계열 | 건수 | 세션 | 진단 |
|---|---|---|---|---|
| 1 | **advisory 경고 스팸** — `[develop-fe] permission_mode=bypassPermissions…` 문구가 트리거 조건(첫 테스트 파일 Edit)과 무관하게 **모든 Edit/Write마다** 발화 | 31+(3b8e), 유사 다수(0101) | 2 | 테스트 파일 Edit 0건인 세션에서도 매번 반복 — 컨텍스트 오염·신호 둔감화. 오탐 |
| 2 | **lint-file.sh 반복 차단** — max-lines=300에 301~306줄이 오르내리며 클러스터 차단, react-hooks/immutability 재발 | 13(0101 8 + 3b8e 5) | 2 | 파일 분리 유도가 아니라 미세 조정 루프 유발. `concern-chat-screen.tsx` 단일 파일에 집중 |
| 3 | **훅 오탐 deny** ①`[protect]` hooksPath **조회**를 substring만으로 차단 ②`[tdd]`가 명령 형태(`perl -pi`)만 보고 대상(소스 파일)을 안 봐 정당한 변이 확인 차단 ③protect-bash 인터프리터 `-c` 가드가 read/write 미구분(python json.load 조회 차단) | 3 | 0101·2f2c | 셋 다 문자열 매칭이 명령 구조를 안 봐서 생김. 정탐 1건(719e: 테스트 파일 `rm` 차단)은 정상 동작 |
| 4 | **Stop 훅이 Q&A 턴에도 풀 verify** → 무관한 브랜치 잔여 실패(debug-step3.test.tsx)를 표면화 → 모델이 스코프 확장("그냥 질문이야" 후에도 재시도) → 인터럽트 ×2 | 1계열(인터럽트 2) | 719e | workflow 3-1은 "Stop → verify(**코드 편집 턴만**)"인데 훅 구현이 이를 안 지킴 — 스펙-구현 불일치 |
| 5 | **리뷰를 raw Agent로 즉석 호출** — L1 리뷰 3회 중 2회를 review-fe 스킬 없이 Agent 직접 호출, PreToolUse:Agent 가드는 advisory라 3/3 탐지·0/3 차단 | 2 | 3b8e | 2026-08-21에도 2회 연속 이탈 기록(workflow 본문 명시) → 누적 4회. advisory로는 안 잡힘 |
| 6 | **증거 캡처 씨름** — Android 키보드 도킹 스크린샷 1장에 ~18분(APK curl 타임아웃·adb 좌표 추측·mock 연결 탐침), macOS osascript 창 포지셔닝 3연속 실패, 끝내 인터럽트로 미완 | 2 클러스터(~11분+18분) | 3b8e | `evidence.screenshot` 선언은 iOS 레시피만 실측 — Android 재현 스크립트·레시피 부재 |
| 7 | **commitlint 한글 충돌이 CI에서야 발견** — header 100자·body-max-line-length 기본값에 한글 커밋 7개 위반, 로컬 게이트에서 안 잡히고 PR CI에서 실패 | 1(수정 커밋 1) | 0101 | subject-case 해제(08-25)와 같은 계열의 잔존 기본값. 보호 파일이라 사용자 지시로 완화 |
| 8 | **플러그인 로컬 경로 미문서화** — 스킬 원본 위치를 marketplaces→installed_plugins.json→`.claude` 순서로 추측하다 6번째 전략(mdfind)에서 발견, Bash 13회·70초 | 1 | 2f2c | 표준 레지스트리에 없는 로컬 개발 설치 — 스킬 내부를 설명할 일이 있을 때마다 반복될 마찰 |
| 9 | **dev 환경 갭** — 실기기에서 localhost baseUrl이 기기 자신을 가리킴(발견 즉시 패치 0bfbce1), Metro/mock 연결을 매번 lsof/netstat로 탐침 | 1계열 | 3b8e | 레포에 dev 연결 레시피 문서 없음 |
| 10 | **(본체 보완) CI 지적 조치 루프 vs 사용자 비용 감각** — 2라운드 지적 10건에 "기계적 전부 반영" 규약대로 진행하자 사용자 축소 지시 3연속("고칠만한 거만" → "api 등 굳이인 건 안 고치고 싶다" → "내 말 듣고있니?"), 이미 적용한 API 계약 수정 1건 번복(revert) | 정정 3+번복 1 | 0101 후반 | 무인 루프 기준(기계적=전부)이 Tier-1 후속 작업의 사용자 기대(기능만)와 어긋남. mid-turn 메시지가 행동 이후에 도착하는 타이밍 문제가 체감을 악화 |

**긍정 신호**(유지할 것): 티어 판정→스펙→red→구현→증거→리뷰→커밋 순서 이탈 0(0101) · red 게이트가 실패 출력 원문과 함께 전 커밋에서 성립 · CI 라운드 5회 한도 내 종료(2/5, 직전 스프린트의 5라운드 미수렴 대비 개선) · 훅 차단 시 재시도 없이 즉시 경로 전환 · PR 답글 커밋 해시 오기 자가 발견·정정.

## 재검토 조건 대조 (workflow.md 7장)

| 조건 | 판정 | 근거 |
|---|---|---|
| 훅을 우회하는 습관 → 훅 범위 축소 | **발동**(우회가 아니라 오탐이 원인) | 집계 #1·#3 — 오탐 4계열이 어제 retro "훅 오탐 12회 중 2계열 잔존"에서 오히려 늘어남(4계열) |
| change당 커밋 10개 초과 2번 | **발동**(2회차 — SIM-139 42개에 이어 SIM-145 브랜치도 초과) | 0101 세션만 15+ 커밋. 단, 초과분의 주범이 CI 리뷰 라운드(라운드당 red+fix 2~3커밋)라 0019 단계-커밋 규칙과는 다른 원인 |
| 스펙 작성 > 구현 2번 | 미발동 | Tier-1 위주라 스펙 비중 낮음 |
| 리뷰 지적 3회 연속 스타일뿐 | 미발동 | CI 2라운드가 기능 버그 1건(useIsMutating 전역) 잡음 |
| 브라우저 계층 flaky 주 2회 | 해당 없음(RN 단일 계층) | — |
| 같은 실패 docs/solutions 2번 | 데이터 부족 | docs/solutions 대조 미수행 |
| 오래된 스냅샷 잘못 구현 / Figma 50회 | 미발동 | Figma 호출 0회 |

## ADR 후보

| # | 제목 | 한 줄 근거 | 고칠 파일 |
|---|---|---|---|
| 1 | 훅 오탐 4계열 정밀화 — 문자열 매칭을 명령 구조 매칭으로 | 집계 #1·#3·#4: advisory 스팸 31회+deny 오탐 3건+Stop 훅 스펙 불일치. hooksPath는 변경 형태만, tdd는 대상 경로 결합, 인터프리터 가드는 read/write 구분, bypassPermissions 경고는 세션당 1회, Stop verify는 코드 편집 턴 한정(스펙대로) | 프로젝트 `.claude/hooks/*` + develop-setup 훅 템플릿 |
| 2 | PreToolUse:Agent 리뷰 가드 하드화(allowlist 방식) | 집계 #5: 누적 4회 이탈을 advisory가 0회 차단. `cgamja:reviewer-*` subagent_type은 허용(스킬 내부 경로), 그 외 리뷰성 Agent는 deny | hooks/skill_guard 계열 · adr/0012 개정 |
| 3 | CI 지적 조치 루프에 규모 게이트 — 라운드 2+는 "기능 영향" 지적만 기본 반영 | 집계 #10: "기계적 전부 반영" 무인 규약이 사용자 정정 3연속·수정 번복 1회를 유발. 라운드 2부터는 기능 영향만 무인 반영하고 나머지는 보류 목록 제안 후 확인 | workflow.md 6장(CI 루프) — 반영은 ADR 확정 후 |
| 4 | Android 증거 캡처 레시피 스크립트화(또는 명시적 스킵 조건) | 집계 #6: 스크린샷 1장에 18분·미완·인터럽트. iOS처럼 검증된 레시피를 `evidence.screenshot`에 싣거나, Java/AVD 미비 환경은 "iOS + 단위 테스트로 대체" 조기 판정 | references/evidence-capture.md(adr/0021 개정) · 프로젝트 cgamja.json |
| 5 | max-lines 임계 vs 화면 컴포넌트 현실 — 차단이 아니라 분리 유도가 되게 | 집계 #2: 300줄 상한에서 13회 반복 차단, 결과가 파일 분리가 아니라 줄 수 미세 조정(ref 통합·JSX 압축)이었음. 상한 상향이 아니라 "차단 시 분리 후보 제시" 또는 화면 컴포넌트 예외 규칙 검토 | 프로젝트 eslint 설정 + project-conventions.md |

## lens-ledger 제안 행 (직접 쓰지 않음 — adr/0016)

- `2026-08-26 | SIM-145 후속(0101f9e8) | L1(opus persona) ×2 | 1차: blocker 0·should 2(1 기계 반영·1 보류) / 재실행: should 1(assertion 보강·변이 확인으로 닫음) | 유효 — 딥링크 뒤로가기 실버그 잡음`
- `2026-08-26 | SIM-145 후속(3b8e61e1) | L1 raw Agent ×2 (스킬 우회 — 무효 표본) | 리뷰 통과 후 같은 린트 오류 3회 재발 | 가드 하드화 근거`

## 프로젝트 세팅 이관 (앱 레포 몫 — 스킬 아님)

- Stop 훅·protect-bash·lint-file 오탐 수정(ADR 1 확정 시 `~/app/.claude/hooks/`에 반영)
- dev 연결 레시피(Metro/mock/실기기 baseUrl) 문서화 — 0bfbce1 커밋 본문이 사실상의 초안
- 잔여 디버그 테스트 파일(untracked)이 무관 세션 verify를 오염 — 세션 종료 시 untracked 테스트 파일 점검 습관

## 다음

- ADR 후보 5개 중 1·2번이 마찰량 기준 최우선(오탐이 전체 마찰의 ~70%). 반영은 사람이 ADR 새 번호로.
- 커버 못 한 세션: 779c0f99(21MB, develop-fe 미로드 — 키보드 스크롤 작업 추정)는 규칙상 제외. 다음 retro에서 스킬 미로드 대형 세션 처리 방침 결정 필요.
