# retro 2026-08-27 (보완) — 세션 0101f9e8 단독 전체 분석

**판정: 세션 1개(develop-fe 본작업 55분, 전체 wall-clock 9h8m — 야간 공백 포함) · 마찰 신호 8종 집계 · 재검토 조건 발동 1개(기존 발동의 corroboration) · 신규 ADR 후보 0개(기존 후보 2건 보강, 관찰 2건은 재검토 조건 미충족)**

## 배경
`reports/retro-2026-08-27.md`(세션 4개 병렬 감사)에서 세션 0101f9e8은 "진행 중 스냅샷"으로만 다뤄졌다(CI 리뷰 라운드 초반까지). 이 리포트는 사용자 요청으로 **같은 세션을 파일이 다 자란 시점 기준 처음부터 재집계**한 것이다 — sonnet 서브에이전트 1개(단일 세션이라 병렬화 불필요) 직접 파싱.

## 세션 개요
- 요청: SIM-145 후속 Tier-1(걱정 대화 화면 자동저장 표시 제거·헤더 좌상단 뒤로가기·"부모님만 봐요" 배지 우측 이동)
- develop-fe 본작업(17:01~17:56 KST 기준, API 사용시간 65.6분 중 55분): spec → red(test-fe) → feat → evidence → L1 리뷰(reviewer-correctness, opus) → 리뷰 지적 반영 → mutation 검증 → push → CI 리뷰 1라운드 대응 → openspec archive
- 17:56 이후는 이 세션 자신을 포함해 4개 세션을 감사하는 `/retro-fe` 메타 작업으로 전환(develop-fe 워크플로 밖)
- 산출물: PR #4에 커밋 13개(앱) + cgamja-plugin 1개, CI 리뷰 1라운드 통과, openspec 아카이브 완료. PR 머지는 트랜스크립트 범위 밖(사람 승인 대기)

## 신호별 집계

| # | 신호 | 횟수 | 분해 | 비고 |
|---|---|---|---|---|
| 1 | 훅 deny/ask | 4 | deny 2(**둘 다 오탐**) + ask 2(정상 승인) | deny①: `git config core.hooksPath` 읽기 전용 조회를 `[protect]`가 문자열만 보고 차단. deny②: 구현파일(`concern-chat-screen.tsx`) 변이 테스트용 `perl -pi`를 `[tdd]`가 명령 형태만 보고 오적용 차단(모델이 Edit로 정상 우회). ask 2건은 `commitlint.config.js`/`tailwind.config.js` 보호파일 편집 승인 |
| 2 | 인터럽트·정정·되물음 | 리터럴 인터럽트 0 / **absorbed_mid_turn 8 + 되돌림 1** | 17:44~17:55 집중. "재검토 꼭 해야돼?" 반복 3회, "내 말 듣고있니?" 1회 | `api/openapi.yaml` 편집을 끝낸 뒤에야 "api는 안 고쳐도 됨" 메시지가 처리돼 `git checkout`으로 되돌림. 모델이 SendFeedback으로 버그 신고(메시지 지연 반영) |
| 3 | 동일 도구+입력 연속 2회 이상 실패 | 0(훅 제외) | — | 재시도 루프 없음 |
| 4 | 게이트·절차 이탈 | 0 | — | 티어판정→spec→red→feat→evidence→L1리뷰→재-red→fix→push→CI대응→re-review→archive 순서 준수. `--no-verify`/`git clean -f`/hooksPath 우회 시도 0건 |
| 5 | 도구 오류(is_error) | 7 | 훅 차단 2(①과 중복) + **`echo ===LABEL===` 셸 구문 오류 5회** | 같은 구분자 관용구가 "not found"로 5회 반복 실패(라인 580/701/882/930/1015) — 앞뒤 실제 명령 출력은 정상 캡처돼 실피해는 적음 |
| 6 | 시간 | develop-fe 55분 / 전체 9h8m | spec+정리 5분·red+feat+evidence 7분·L1 1차 즉시·지적반영 1분·L1 재실행 즉시·mutation+push 15분·**CI 진단 4분**·CI라운드 대응 12분·아카이브 1분 | 이후 retro-fe 메타작업 18분 + 익일 push 1건 |
| 7 | 커밋·PR·승인 | 커밋 14(앱13+플러그인1) / PR 신규 0(#4 기존) / ask 2 / re-review 라벨 1 | Jira `[예외]` 댓글 2건(17:26, 17:47) 정상 부착 | — |
| 8 | 리뷰 렌즈 | Agent 호출 2회, 전부 `reviewer-correctness`(L1) | 1차(spec+test+feat diff), 재실행(지적 반영분만) | Tier-1이라 L1만 호출은 adr/0012 라우팅대로 정상. L1 지적("하드웨어 뒤로가기 goBack 공유")은 즉시 fix 커밋 반영. 별도 GitHub Actions CI 리뷰 1라운드는 기계적 지적 다수를 커밋 반영, 판단성 지적(배너색 등)은 PR 답글로 보류 후 종료 — 3단계 규약대로 |

## 마찰 3분류
| 항목 | 분류 | 근거 |
|---|---|---|
| 훅 오탐 2건(①) | **프로젝트 세팅** | `[protect]`가 read/write 미구분, `[tdd]`가 명령 형태만 보고 대상 파일 미확인 |
| absorbed_mid_turn 8 + 되돌림 1(②) | **스킬 설계** | CI 지적 반영 루프가 여러 파일을 연속 편집하도록 짜여 있어 그 사이 사용자의 스코프 축소 메시지를 받을 체크인 지점이 없음 |
| `echo ===` 구문 오류 5회(⑤) | **모델 행동** | 같은 셸 관용구 실패를 학습 없이 반복(피해 미미) |
| CI 진단 4분(⑥) | **프로젝트 세팅** | commitlint 헤더/본문 길이 제한이 한글 커밋 관행과 불일치 — `retro-2026-08-27.md` #7과 동일 원인 |

## workflow.md 7장 재검토 조건 대조
| 조건 | 판정 | 근거 |
|---|---|---|
| change당 커밋 10개 초과 2번 | **발동(기존 발동 보강)** | 이 세션 단독 13~14커밋 — `retro-2026-08-27.md`가 이미 "2회차 발동"으로 표시한 SIM-145 브랜치 건의 실측치 확인. 새 트리거 아님 |
| 훅을 우회하는 습관이 생김 | 미발동 | 우회 시도 0건 — 오탐이어도 모델은 정상 대체 경로(Edit) 사용 |
| 리뷰 지적 3회 연속 스타일뿐 / 렌즈 "없음" 3연속 | 미발동 | L1 지적이 실질 버그(하드웨어 뒤로가기) 잡음, Tier-1 라우팅상 L1 단독 호출은 정상 |
| sonnet 구현 2회 연속 실패 | 미발동 | 0회 |
| 나머지 조건(스펙-테스트 모순, flaky, 도메인 엣지, 디자인 갭 재손봄, Figma 50회/일 등) | 데이터 없음/N차 | 이 세션 범위 밖 |

## ADR 후보 갱신
- **신규 후보 없음.** 훅 오탐 2건은 `retro-2026-08-27.md` ADR 후보 #1("훅 오탐 4계열 정밀화")의 근거를 2건 더 보강 — 별도 제안 아님.
- **관찰(재검토 조건 미충족, 다음 retro에서 재현 시 후보화)**:
  1. CI 지적 반영 루프의 mid-turn 메시지 체크인 부재(absorbed_mid_turn) — 이번이 1회째 기록. workflow.md 7장에 대응 조건 없음 — 재현 시 "CI 대응 루프는 파일 편집 사이 사용자 메시지를 먼저 소진" 류 조건 신설 검토.
  2. `echo ===LABEL===` 구문 오류 반복 — 이번 세션 내 5회지만 세션 간 재현 미확인. 다음 retro에서 재현되면 "같은 실패 2회 → 린트/훅/config로 승격" 조건 적용 검토.

## 기존 리포트와의 정합성
`retro-2026-08-27.md`가 이 세션을 스냅샷으로 언급한 항목 중 "advisory 경고 스팸 31회+", "lint-file.sh max-lines 8회"는 **이번 전체 재집계에서 재현되지 않음**(이번 세션의 훅 관련 이벤트는 4건뿐 — 해당 스팸·차단은 다른 세션(3b8e)의 것으로 재확인 필요, `retro-2026-08-27.md`의 세션별 분해 표를 참고). "opus 리뷰 2회"·"commitlint 한글 충돌 CI 실패 1건"은 일치.
